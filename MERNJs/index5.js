//objects-->collections of mutliple date in the form of keys
//let a=["john",23,"developer"]--array

/*let obj={
    name:"john",
    age:23,
    role:"developer"
}
// console.log(obj);
console.table(obj)
*/
/*let employee={
    name:"karthik",
    age:22,
    role:"developer",
    skills:["html", "javascript", "css"],
    // name:"sheldon"//re-initialize done inside the object also
    addresses:{  //nested object was created //we cannot write in a singlr line because we dont no the values so we create a another object
        number:30,
        street:10,
        city:"hassan",
        state:"karnataka",
        pincode:573217
    }
}
// employee.name="John" //re-initialize
// employee.skills="javascript"//it will inserted into the object
// employee.emial="karthi@gamil.com"
// console.log(employee["name"])// we can return (employee.name) it is also posible
// console.table(employee)
console.table(employee.addresses.city)*/

let s="Karthik"
let n=4
for (let i=0; i<n; i++){
console.log(s.substring(0,n))
}